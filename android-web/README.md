# Sketch Magic Android web launcher

A Trusted Web Activity APK, not a native drawing rewrite or a Chrome-generated WebAPK. It renders the hosted PWA in Chrome, preserving Chrome storage and permissions. Digital Asset Links authorize fullscreen presentation; if validation fails Chrome shows its URL bar. No provider credentials or microphone permissions live in this wrapper. This build is for the owner's connected Android 13 tablet.
