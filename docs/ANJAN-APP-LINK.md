# anjan.app/incant

The owner requested the short address https://anjan.app/incant on 2026-09-11. It follows anjan.app's existing convention: a 302 redirect opens https://incant-web-production.up.railway.app/. The browser address changes to Railway; this is not a second Incant deployment or an embedded copy.

Keeping the existing origin retains local storage and the installed DC-1 app's connection to the same archive. Incant's passphrase remains in force. The short link is public without requiring the separate anjan.app index cookie; no other index gate is changed. Only view/frame/layer/mode query parameters are forwarded, never the index's unlock capability.

Router repository: a12k-a2b/anjan-app, commit d72e312. Working copy: /Users/anjan/Documents/SolOS/anjan-app-incant-route. The original /Users/anjan/code/anjan-app checkout was left untouched. Source was pushed to main through the established automatic Railway deployment workflow. Changes consist of a curated registry entry and an exact-path redirect before the index gate. Route tests include unauthenticated index/API protection, method handling, fixed destination, and query filtering.

No Incant product code, provider keys, cloud volume, drawing data, or native APK changed for this request.
