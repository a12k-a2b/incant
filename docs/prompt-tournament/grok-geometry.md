Geometry-first contract: the sketch is locked spatial anchors (frame, inventory including repeated parts, placement, silhouette envelopes, occlusion, negative space) that must register if overlaid on the result. The spell is an appearance shader inside those envelopes; explicit structural requests edit only their named scope. Ambiguity uses least-geometry-change so rough handmade marks survive as the user's drawing, fully rendered rather than replaced.