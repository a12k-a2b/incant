import {chromium} from '@playwright/test';
const browser=await chromium.launch({headless:true,executablePath:process.env.CHROME_PATH});try{const page=await browser.newPage();await page.goto('http://127.0.0.1:5173');await page.evaluate(()=>{(window as any).focusTrail=[];document.addEventListener('focusin',e=>{const t=e.target as HTMLElement;if(t.closest('.type-bubble'))(window as any).focusTrail.push(t.getAttribute('aria-label')||t.tagName);});});await page.getByRole('button',{name:'Type a spell',exact:true}).click();console.log(await page.evaluate(()=>(window as any).focusTrail));}finally{await browser.close();}
